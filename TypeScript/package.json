{
    "name": "design-pattern",
    "version": "0.1.0",
    "description": "Practice and make examples about design patterns.",
    "type": "module",
    "scripts": {
        "rimraf": "./node_modules/rimraf/bin.js",
        "clean": "rimraf ./.build",
        "build": "tsc --incremental",
        "rebuild": "npm run clean && npm run build",
        "node": "node --experimental-modules",
        "start": "npm run build && npm run node",
        "start-clean": "npm run rebuild && npm run node",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "DivineCross",
    "license": "ISC",
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^2.23.0",
        "@typescript-eslint/parser": "^2.23.0",
        "eslint": "^6.8.0",
        "minimist": ">=1.2.2",
        "rimraf": "^3.0.2",
        "typescript": "^3.8.3"
    }
}
